import { defineConfig } from 'vite'

// Replace 'your-repository-name' with your actual repository name
export default defineConfig({
  base: '/',  // Change this to '/' if you're using a custom domain or '/your-repository-name/' if not
})